# ------------------------------------------------------------------------------
# App Config
# Use .env-example as a template for your other .env environments
# ------------------------------------------------------------------------------
APP_VERSION=0.0.1
APP_NAME="gairos"
APP_PORT=8000
# used for password hashing
APP_SECRET=CHANGE_ME
APP_SESSION_SECRET=CHANGE_ME
APP_SALT_ROUNDS=10

# ------------------------------------------------------------------------------
# JWT Config
# ------------------------------------------------------------------------------
# should be a minimum of 32 bytes
JWT_SECRET=CHANGE_ME
JWT_ISS=http://localhost
JWT_EXPIRES_IN=3600

# ------------------------------------------------------------------------------
# Test Config
# ------------------------------------------------------------------------------
# IMPORTANT: tests are built with this being ON (1) in mind!
DROP_COLLECTIONS_BEFORE_EACH_TEST=1

# ------------------------------------------------------------------------------
# Dabase Config
# Important: if using docker, config/creds must match those in docker-compose
# ------------------------------------------------------------------------------
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=54320
DB_NAME=development
# sequelize options
DB_SYNC_WITH_SEQUELIZE=false
DB_DIALECT=postgres
FAIL_BUILD_IF_SEEDERS_FAIL=true

# ------------------------------------------------------------------------------
# Social Config
# ------------------------------------------------------------------------------
GOOGLE_CLIENT_ID=606030648640-av4cpgpssu2jbev1ut1atsrhc4hluflp.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=NSZcfR7qERKqWVhtv4HvcCBA
GOOGLE_CALLBACK_URL=http://localhost:8000/auth/google/cb