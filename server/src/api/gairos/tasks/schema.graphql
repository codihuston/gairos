extend type Query {
  getTasks: [Task]
  getUserTasks(userId: ID!): [UserTask]
}

type Task {
  id: ID!
  name: ID!
  userTask: UserTask
}

type UserTask{
  id: ID!
  name: String!
  description: String
  isPublic: Boolean
}